# ingestion/

Put your Python data-loading code here. It should read the raw files in `../data/` and load them
into your DuckDB warehouse so dbt can build on top of them. Structure it however you think best.
